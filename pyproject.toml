[project]
name = "mailbot"
version = "0.9.1"
description = "Personal IMAP email automation system"
requires-python = ">=3.10"
dependencies = [
    "lxml",
    "html2text",
]

[project.optional-dependencies]
dev = ["pytest"]
